name: Teste de email
on:
  workflow_dispatch: # Executar todos os dias às 00:00 UTC

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: SendGrid
      uses: peter-evans/sendgrid-action@v1
      env:
        SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
        SCRIPT_FILEPATH: ./Email/email.js
